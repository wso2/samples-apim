import { Optional } from '@stoplight/types';
import { Resolver } from '@stoplight/spectral-ref-resolver';
export declare const getResolver: (resolver: Optional<string>, proxy: Optional<string>) => Resolver;
